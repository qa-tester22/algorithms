// Дано: массив строк:
// Вернуть: объект, где ключи - элементы массива, значения - количество вхождения строк
const stringList = ['Java', 'Python', 'JS', 'Java', 'JS', 'Python', 'C#', 'Java']

function groupStringObj (array) {
    let newObj = {}
    let temtArr = array.filter(el => indexOf(el) !== indexOfLast)
}

let string = stringList.filter(el => stringList.indexOf(el) !== stringList.lastIndexOf(el))
console.log(string);